# Aurora / Daily Tech Hub on Railway
# Dashboard: create service from this GitHub repo, attach a volume at /data

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/"
healthcheckTimeout = 30
